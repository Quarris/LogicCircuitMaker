using Microsoft.Xna.Framework;

namespace LCM {
    public class Robot {
        private Vector2 position;
        public Vector2 Position {
            get => this.position;
            set => this.position = value;
        }
    }
}